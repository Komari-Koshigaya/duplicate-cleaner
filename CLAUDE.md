# Duplicate Cleaner

## 项目背景

重复文件查找与清理工具，用于扫描指定目录下内容相同但文件名不同的文件，支持图形界面操作，一键清理重复文件释放磁盘空间。

## 技术栈

- Python 3.8+
- tkinter + ttkbootstrap（现代 GUI）
- 标准库：hashlib、threading、concurrent.futures、pathlib、json、csv
- send2trash（安全删除，移到回收站）

## 环境要求

- Python 3.8+
- 无需虚拟环境

## 运行命令

```bash
# 图形界面版
python duplicate_cleaner_gui.py

# 命令行版
python duplicate_cleaner.py /path/to/directory
```

## 项目结构

```
duplicate-cleaner/
├── CLAUDE.md                   # 项目说明（本文件）
├── README.md                   # 用户文档
├── .gitignore                  # Git 忽略规则
├── duplicate_cleaner.py        # 命令行版
└── duplicate_cleaner_gui.py    # 图形界面版（主推）
```

## 核心逻辑

1. 按文件大小预筛选（大小不同不可能重复）
2. 快速哈希（读取前 64KB）进一步筛选
3. 对可疑文件计算完整 MD5 哈希确认
4. 多线程并行计算（ThreadPoolExecutor）

## 已完成功能

- [x] 双击打开文件（系统默认程序）
- [x] 右键菜单：打开文件、打开所在目录、复制路径
- [x] 删除前安全检查：确保每组至少保留一个文件
- [x] 菜单栏：文件 → 选择目录、导出扫描结果、退出
- [x] 菜单栏：视图 → 窗口大小/字体大小（小/中/大）、恢复默认
- [x] 菜单栏：设置 → 单实例模式、文件类型过滤、扫描完成提示音
- [x] 菜单栏：帮助 → 使用说明、快捷键、关于
- [x] 快捷键支持：Ctrl+O/S/E/Q、F1、F5
- [x] 列头排序：点击组号、大小、路径列头可排序
- [x] 安全删除：支持移到回收站或永久删除
- [x] 配置保存：自动保存窗口大小、字体、目录等设置
- [x] 状态栏：显示详细状态和快捷键提示
- [x] 文件类型过滤：只扫描图片/视频/音频/文档/压缩包
- [x] 单实例模式：可配置是否只允许一个窗口实例
- [x] 工具栏：导出结果、搜索文件
- [x] 搜索过滤：输入关键词实时过滤结果
- [x] 最近目录：下拉菜单显示最近扫描的目录
- [x] 自动选中：扫描完成自动选中推荐删除的文件

## 待办事项

- [ ] 支持选择哈希算法（MD5/SHA256/SHA1）
- [ ] 支持文件预览（图片、视频缩略图）
- [ ] 添加深色模式
- [ ] 支持拖拽目录到窗口
- [ ] 添加扫描历史记录
